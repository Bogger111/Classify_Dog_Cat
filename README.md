# Classify_Dog_Cat

## 项目简介

本项目是一个基于 PyTorch 的猫狗图像分类项目，使用自定义 ResNet18 网络结构完成二分类任务。

项目主要目的是熟悉深度学习图像分类任务的完整流程，包括数据读取、数据增强、模型搭建、训练验证、模型保存和结果可视化。

## 项目特点

- 使用 PyTorch 搭建训练流程
- 实现自定义 ResNet18 模型
- 支持训练集与验证集划分
- 支持模型 checkpoint 保存
- 支持训练曲线、混淆矩阵和预测样例可视化
- 支持通过配置文件管理训练参数

## 技术栈

- Python
- PyTorch
- Torchvision
- NumPy
- Matplotlib
- scikit-learn
- tqdm
- YAML

## 模型结构

本项目使用自定义实现的 ResNet18 结构，包含 BasicBlock、残差连接、卷积层、BatchNorm、ReLU 和全连接分类层。

相比直接调用 torchvision.models，本项目更注重对 ResNet 网络结构和训练流程的理解。

## 数据集

数据集来源于 Kaggle Dogs vs Cats 图像分类任务。

任务目标是根据输入图片判断其类别为猫或狗。

## 训练流程

主要训练流程包括：

1. 读取图像数据
2. 数据预处理与增强
3. 构建训练集与验证集
4. 初始化模型、损失函数、优化器和学习率调度器
5. 训练模型并记录 loss 和 accuracy
6. 保存验证集表现最好的模型
7. 绘制训练曲线和混淆矩阵

## 当前结果

目前模型在验证集上取得了约 94% 左右的准确率。

该结果说明模型已经能够较好地区分猫狗图像，但仍存在进一步优化空间，例如：

- 使用更强的数据增强
- 调整学习率策略
- 尝试预训练模型
- 增加错误样本分析
- 优化模型泛化能力

## 项目收获

通过本项目，我主要熟悉了：

- PyTorch 项目的基本组织方式
- CNN 和 ResNet 的基本原理
- 图像分类任务的完整训练流程
- 训练日志、验证指标和可视化分析方法
- 深度学习项目中配置文件、checkpoint 和结果管理的基本方法

## 后续计划

- 增加测试集预测功能
- 完善 README 中的实验结果展示
- 添加更多训练曲线和预测样例
- 尝试迁移学习方法
- 对错误分类样本进行分析
